import React, { useEffect } from 'react';
import { Button } from 'react-bootstrap';

import './Counter.scss';

const Counter = ({ tokenPrice, numberOfTokens, setNumberOfTokens }) => {
  return <></>;
};

export default Counter;
